@extends('admin.layouts.app')
@section('title', 'Enquiry Details')
@section('content')
<div class="page-header"><h1 class="page-title">Enquiry Details<span class="subtitle">{{ $enquiry->name }}</span></h1><div class="d-flex gap-2"><a href="{{ route('admin.enquiries.edit', $enquiry) }}" class="btn btn-primary"><i class="bi bi-pencil me-1"></i>Edit</a><a href="{{ route('admin.enquiries.index') }}" class="btn btn-outline-secondary">Back</a></div></div>
<div class="row g-4"><div class="col-lg-7"><div class="admin-card mb-4"><div class="admin-card-header"><h5>Customer Message</h5></div><div class="admin-card-body"><div class="row g-3 mb-3"><div class="col-md-6"><div class="small text-muted">Email</div><div class="fw-semibold">{{ $enquiry->email }}</div></div><div class="col-md-6"><div class="small text-muted">Mobile</div><div class="fw-semibold">{{ $enquiry->mobile }}</div></div><div class="col-md-6"><div class="small text-muted">Party Size</div><div class="fw-semibold">{{ $enquiry->party_size ?: '-' }}</div></div><div class="col-md-6"><div class="small text-muted">Status</div><div class="fw-semibold">{{ ucfirst($enquiry->status) }}</div></div></div><div class="small text-muted">Message</div><div class="fw-semibold">{{ $enquiry->message ?: 'No message provided.' }}</div></div></div><div class="admin-card"><div class="admin-card-header"><h5>Reply</h5></div><div class="admin-card-body"><form action="{{ route('admin.enquiries.reply', $enquiry->id) }}" method="POST">@csrf<label class="form-label">Admin Reply</label><textarea name="admin_reply" class="form-control" rows="6" required>{{ old('admin_reply', $enquiry->admin_reply) }}</textarea><button class="btn btn-primary mt-3" type="submit">Send Reply</button></form></div></div></div><div class="col-lg-5"><div class="admin-card"><div class="admin-card-header"><h5>Response Status</h5></div><div class="admin-card-body"><div class="d-flex justify-content-between py-2 border-bottom"><span class="text-muted">Received</span><strong>{{ $enquiry->created_at?->format('d M Y, h:i A') }}</strong></div><div class="d-flex justify-content-between py-2 border-bottom"><span class="text-muted">Replied At</span><strong>{{ $enquiry->replied_at?->format('d M Y, h:i A') ?? 'Not replied' }}</strong></div><div class="pt-3"><div class="small text-muted">Latest Reply</div><div class="fw-semibold">{{ $enquiry->admin_reply ?: 'No reply sent yet.' }}</div></div></div></div></div></div>
@endsection
