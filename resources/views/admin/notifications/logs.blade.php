@extends('admin.layouts.app')
@section('title', 'Notification Logs')
@section('content')
<div class="page-header"><h1 class="page-title">Notification Logs<span class="subtitle">Audit trail for SMS and email broadcasts</span></h1><a href="{{ route('admin.notifications.index') }}" class="btn btn-outline-secondary"><i class="bi bi-arrow-left me-1"></i>Back to Broadcast</a></div>
<div class="admin-card mb-4"><div class="admin-card-body"><form class="row g-3 align-items-end"><div class="col-md-3"><label class="form-label">Type</label><select name="type" class="form-select"><option value="">All</option><option value="sms" @selected(request('type')==='sms')>SMS</option><option value="email" @selected(request('type')==='email')>Email</option></select></div><div class="col-md-3"><label class="form-label">Status</label><select name="status" class="form-select"><option value="">All</option><option value="sent" @selected(request('status')==='sent')>Sent</option><option value="failed" @selected(request('status')==='failed')>Failed</option><option value="pending" @selected(request('status')==='pending')>Pending</option></select></div><div class="col-md-6"><button class="btn btn-primary me-2" type="submit">Filter</button><a href="{{ route('admin.notifications.logs') }}" class="btn btn-outline-secondary">Reset</a></div></form></div></div>
<div class="admin-card"><div class="table-responsive"><table class="table-admin"><thead><tr><th>User</th><th>Type</th><th>Status</th><th>Template</th><th>Sent At</th><th>Payload</th></tr></thead><tbody>@forelse($logs as $log)<tr><td><div class="fw-semibold">{{ $log->user?->name ?? 'Unknown User' }}</div><div class="small text-muted">{{ $log->user?->email ?? '-' }}</div></td><td class="text-uppercase">{{ $log->type }}</td><td><span class="status-badge {{ $log->status === 'sent' ? 'status-active' : ($log->status === 'failed' ? 'status-blocked' : 'status-pending') }}">{{ ucfirst($log->status) }}</span></td><td>{{ $log->template ?: '-' }}</td><td>{{ $log->sent_at?->format('d M Y, h:i A') ?? '-' }}</td><td><code class="small">{{ json_encode($log->payload) }}</code></td></tr>@empty<tr><td colspan="6" class="text-center py-4 text-muted">No notification logs found.</td></tr>@endforelse</tbody></table></div>@if($logs->hasPages())<div class="px-4 py-3 border-top">{{ $logs->links() }}</div>@endif</div>
@endsection
